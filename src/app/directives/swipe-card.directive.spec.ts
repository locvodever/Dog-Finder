import { SwipeCardDirective } from './swipe-card.directive';

describe('SwipeCardDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeCardDirective();
    expect(directive).toBeTruthy();
  });
});
